<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.istic.mob.horairesbustb">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permissions.READ_DATABASE"/>
    <uses-permission android:name="android.permissioms.WRITE_DATABASE"/>

    <permission android:name="fr.isitc.mob.horairesbustb.provider.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="fr.isitc.mob.horairesbustb.provider.WRITE_DATABASE" android:protectionLevel="normal" />



    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorairesBusTB">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:authorities="fr.istic.mob.horairesbustb.provider"
            android:name=".provider.MyContentProvider"
            android:enabled="true"
            android:exported="true"  />
    </application>

</manifest>